# Test case: <short scenario name>

_A test-case template. For a TMS this is a field map (put each part into the matching
project field — the profile's TMS section); for file mode (`tms: none`) it is the literal
skeleton of a file in `docs/test-cases/` or the path from the profile._

- **Task:** <tracker task id>
- **Requirement:** «<verbatim quote of the spec item / acceptance criterion this case verifies>»
- **Priority:** <per the profile's scale>

## Preconditions

<what must exist before the steps: entities, settings, user role>

## Steps

1. <a step — a skeleton, not a click-by-click recipe: the analyst has never seen the env>
2. <…>

## Expected result

<the reference: a quote or a direct paraphrase of the requirement, in terms of the business effect rather than a response code>

**Expectation source:** spec §<N> | KB <file> | assumption
