---
description: Print a digest of what was tested today (or over N days) and how sessions ended — /qa-day
---

Run the `qa-day` skill (the daily digest) and follow it. Arguments are passed through verbatim
(e.g. `--days 7`, several project roots).

Args: $ARGUMENTS