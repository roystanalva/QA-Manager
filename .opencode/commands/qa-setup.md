---
description: Wire qa-manager into this project and write/update .opencode/qa-profile.md — /qa-setup
---

Run the `qa-setup` skill (qa-manager onboarding) and follow it: update the engine first, then
interview the user and write the project profile per PROFILE-CONTRACT.md.

Goal: $ARGUMENTS